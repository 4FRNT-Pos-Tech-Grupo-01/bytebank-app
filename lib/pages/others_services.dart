import 'package:flutter/material.dart';

class OthersServices extends StatelessWidget {
  const OthersServices({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Others Services Page"));
  }
}
